# 🐾 桌面宠物 (Desktop Pet) 使用与分发指南

欢迎使用**桌面宠物 (Desktop Pet)**！这是一款基于 Electron + Vue 3 + TypeScript 构建的沉浸式桌面宠物应用，集成了 Web3 智能合约进化机制、四维成长打卡以及极致的透明悬浮体验。

---

## ❓ 常见问题：只把 `DesktopPet.exe` 给别人能不能直接用？

**答：不能直接发送单一的 `.exe` 文件！**

### 💡 为什么？
`DesktopPet.exe` 是 Electron 应用的主引导程序，它的运行需要依赖同级目录下的核心资源与依赖库（包括包含业务代码与 UI 的 `resources/app.asar`、Chromium 渲染引擎 DLL 动态链接库等）。如果只提取一个 `.exe` 发送，对方双击时会提示丢失组件或直接闪退。

### ✅ 正确的分发/分享方式：
将打包好的 **`DesktopPet-v0.1.0-win-x64.zip`** 压缩包（位于项目的 `release/` 目录下）直接发送给好友。

**好友的使用步骤**：
1. 收到 `DesktopPet-v0.1.0-win-x64.zip` 后，右键解压到任意文件夹；
2. 打开解压后的文件夹，双击里面的 **`DesktopPet.exe`** 即可直接流畅运行！
3. **无需安装 Node.js、Git 或任何开发环境**，真正的绿色免安装。

---

## 🎮 软件核心功能与使用指南

### 1. 🐭 自由拖拽与自动记忆位置
* **移动宠物**：按住鼠标左键即可拖拽宠物在屏幕任意位置自由移动。
* **位置记忆**：当你将宠物拖拽到满意的桌面上时，程序会自动记录当前坐标，下次启动软件时宠物会自动出现在上次放置的位置。

### 2. 📝 快捷打卡与四维成长
* **开启打卡**：右键点击宠物，或右键电脑右下角的系统托盘图标，选择 **「📝 快捷打卡」**。
* **属性提升**：支持在打卡弹窗中输入目标并选择加点属性（🎯 专注、🧠 智慧、🎨 创造、💛 共情），每次打卡对应属性经验 **+10**。
* **打卡反馈**：打卡成功时，宠物上方会向上升起黄色经验飘字，同时宠物会伴随立体的抖动反馈。

### 3. ✨ 阶段进化与 Web3 链上突破
宠物的成长分为 3 个不同阶段，满足经验条件后将触发 Web3 链上进化：

| 阶段 | 形态 | 进化所需经验 | 说明 |
| :--- | :--- | :--- | :--- |
| **阶段 0** | 宠物蛋 (Stage 0) | **30 经验** | 打卡满 30 经验后，出现「✨ 可以进化了！」按钮。点击触发金光与粒子特效，静默部署/铸造 Web3 NFT 并进化至幼体。进化后经验归 0。 |
| **阶段 1** | 幼体 (Stage 1) | **60 经验** | 进入幼体形态，重新打卡满 60 经验后可再次触发进化至最终形态。进化后经验归 0。 |
| **阶段 2** | 进化体 (Stage 2) | **无上限** | 最终高级形态。经验值无上限（例如 70, 120, 500+），可无限打卡积累，不再出现进化按钮。 |

### 4. 🐾 宠物身份证 (NFT 状态面板)
* **呼出方式**：**双击宠物**（两次点击间隔在 300ms 内）。
* **功能**：面板中展示宠物的当前阶段名、Token ID、Monad 区块链智能合约地址以及四维属性柱状图。点击浏览器链接可跳转到区块链浏览器查看真实的链上交易与合约凭证。

### 5. 🔔 右下角系统托盘区 (Tray)
* **隐形陪伴**：程序开启后不会占据 Windows 底部任务栏，而是隐形驻留在桌面与电脑右下角的系统托盘区。
* **托盘右键菜单**：
  * `📝 快捷打卡`
  * `🔄 重置数据`
  * `👁️ 显示/隐藏宠物`
  * `❌ 退出应用`
* **托盘左键单击**：一键聚焦/显示桌面宠物。

### 6. 🎨 自定义托盘图标
如果你希望修改电脑右下角托盘显示的图标：
1. 准备一张你喜欢的图片（PNG 或 ICO 格式，推荐 32x32 透明底）；
2. 命名为 `icon.png`；
3. 替换掉项目中的 `public/icon.png` 文件，重新打包或启动即可！

---

## 🛠️ 开发与打包指南

```bash
# 启动本地开发调试
npm run dev

# 打包生产环境免安装绿色包
npm run build
```

打包完成后，产物位于 `release/` 目录下：
* `release/win-unpacked/`：解压可直接运行的目录。
* `release/DesktopPet-v0.1.0-win-x64.zip`：可直接发送给好友的绿色 ZIP 压缩包。
